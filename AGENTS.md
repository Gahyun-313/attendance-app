# 프로젝트 작업 지침

## 작업 전 확인

- 요청을 받으면 먼저 이해한 내용, 현재 브랜치, 작업 범위·파일, 예정 커밋 메시지를 설명한다. 사용자가 작업 시작을 요청하면 구현한다. 이미 시작을 승인한 범위는 반복 확인하지 않는다.
- 실제 구현 대상은 이 저장소 `attendance-app`이다. 별도 `attendance-app-claude/LEARNING_GUIDE.md`는 학습 단계 참고 자료다. 두 저장소를 혼동하지 않는다.
- 수정 전에 현재 경로·브랜치·Git 변경 상태와 대상 파일을 확인한다. 기존 사용자 변경을 덮어쓰지 않고 이어서 작업한다.
- 브랜치 생성·전환과 푸시는 사용자가 직접 한다. 명시적으로 별도 요청하기 전에는 수행하지 않는다.

## 코드와 학습 문서

- 실제 소스의 주석은 일반적인 개발 코드 수준에서 조금 더 자세하게 작성한다. 주요 책임, 처리 이유, 주의할 동작을 설명하되 모든 줄에 주석을 붙이지 않는다.
- 작업 대상 기능의 `feature/<기능>/` 아래에 설명 Markdown을 작성하거나 갱신한다. 다른 기능의 문서는 요청 범위를 벗어나면 확장하지 않는다.
- 설명 문서에는 실제 코드의 모든 줄·로직·흐름을 상세 주석으로 해설한다. package/import는 생략할 수 있다. 메서드 바로 위에는 책임, 호출 상황, 처리 순서, 상태 변화와 결과를 묶어서 설명한다.
- 설명 코드는 실제 구현의 동작과 순서를 유지한다. 소스 변경 시 문서도 함께 갱신하며, 문서를 위해 별도 동작을 지어내지 않는다.
- 파일별 책임과 의존 관계, 실제 함수 호출과 상태 구독의 차이, 사용자 이벤트부터 UI 반영까지의 흐름, Kotlin·Compose·Coroutine 문법, 아키텍처 선택 이유, 테스트의 검증 대상을 설명한다.
- 현재 구현과 향후 계획을 구분한다. Fake 인증을 실제 서버 인증으로, 성공 콜백을 Navigation 연결 완료로 표현하지 않는다. 가이드의 후속 Hilt 구조를 현재 수동 DI 구조와 혼동하지 않는다.
- 문서는 코드를 이해하며 개발하기 위한 설명 노트이자 외부 독자도 읽는 프로젝트 문서다. 채용 준비를 직접 드러내는 제목 대신 `이해를 위해 스스로 설명해 볼 질문`처럼 중립적인 표현을 사용한다.
- 사용자가 코드를 보내 설명만 요청하면 상세 주석을 붙인 코드를 응답하되, 실제 파일을 수정하거나 리팩터링하는 권한으로 해석하지 않는다.

## 검증 안내

- 단위 테스트, 계측/UI 테스트, APK 빌드는 사용자가 직접 실행한다. 별도 실행 요청이 없는 한 Gradle 테스트·빌드, 에뮬레이터 시작, APK 설치를 자동 실행하지 않는다.
- 작업 완료 시 실행할 디렉터리, 명령어, JDK·SDK·기기 등 사전 조건, 기대 결과, 결과 보고서 위치를 안내한다.
- 읽기 전용 코드 확인, Git diff 확인, 설명 코드와 소스의 일치 확인은 진행할 수 있다.
- 미실행 항목은 미실행으로 표시한다. 과거 성공 결과와 이번 변경 검증을 구분하고, APK 빌드와 기기 테스트 실행을 별도 결과로 설명한다.

## 커밋

- 커밋을 요청받으면 설명 문서까지 완성한 뒤 관련 변경을 커밋한다. 푸시하지 않는다.
- 학습 가이드의 단계별 작업은 가이드에 있는 파일별 커밋 메시지와 묶음을 따른다.
- 설명 Markdown은 `docs(기능): ...` 형식의 적절한 메시지로 별도 커밋할 수 있다.
- 커밋 전에 대상 파일을 명시적으로 스테이징하고 diff를 확인한다. 완료 후 커밋 내역과 남은 변경을 확인하여 알린다.
